const uri = require('uri-js');
const CHTSH = "https://cht.sh"
CHTSH + "/bash"
function addToString (s) {
if (s) {
  console.log(`${s} found`);
  return `${CHTSH}${s}`;
  }
{
  }
}
addToString
addToString("/bash")